# Vendored 3rd-party ComfyUI node packs (copied in, never git-cloned).
# Each subpackage keeps its upstream LICENSE + attribution. Registered into
# ComfyUI NODE_CLASS_MAPPINGS from the top-level __init__.py.
